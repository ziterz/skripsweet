using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NaikLevel3 : MonoBehaviour
{
    // Start is called before the first frame update
    public void NaikLevel() {
        PlayerPrefs.SetInt("x", 46);
    }
}
