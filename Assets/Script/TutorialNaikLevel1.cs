using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TutorialNaikLevel1 : MonoBehaviour
{
    // Start is called before the first frame update
    public void NaikLevel() {
        PlayerPrefs.SetInt("x", -2);
    }
}
